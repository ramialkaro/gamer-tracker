#!/usr/bin/env node
var serverDirectory="http://www.tutorit.net/courses/templates/modules/";
if (process.argv.length<3) return;
var cp=require("child_process");
var modules=['common'];
for(var i=2;i<process.argv.length;i++) modules.push(process.argv[i]);
console.log(modules);

var setup=require('./utils')(serverDirectory,modules);
setup(serverDirectory,modules);
